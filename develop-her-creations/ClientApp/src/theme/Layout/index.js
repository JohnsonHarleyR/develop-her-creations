import React from "react";
import NavMenu from "./NavMenu";

const Layout = ({}) => {

    return(
        <div>
            <NavMenu />
            Layout here
        </div>
    );
}

export default Layout;